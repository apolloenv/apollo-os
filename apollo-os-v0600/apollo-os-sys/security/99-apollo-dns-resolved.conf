# Apollo OS — NetworkManager DNS via systemd-resolved
# Deployed to: /etc/NetworkManager/conf.d/99-apollo-dns-resolved.conf
# Ensures NetworkManager delegates DNS to systemd-resolved (for DoT support)

[main]
dns=systemd-resolved
