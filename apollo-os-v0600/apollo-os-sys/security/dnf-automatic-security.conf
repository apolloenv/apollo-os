# Apollo OS - Automatic Security Updates
# Deployed to: /etc/dnf/automatic.conf
# Compatible with both dnf4 (dnf-automatic) and dnf5
# Only installs security updates automatically

[commands]
upgrade_type = security
apply_updates = yes
download_updates = yes

[emitters]
emit_via = stdio

[base]
debuglevel = 1
